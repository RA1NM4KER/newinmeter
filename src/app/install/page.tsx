import { InstallPageClient } from "@/components/pwa/install-page-client";

export const metadata = {
  title: "Install NewinMeter"
};

export default function InstallPage() {
  return <InstallPageClient />;
}
